<resources>
    <!-- Base application theme. -->
    <style name="Base.Theme.SparkWeather" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
        <item name="android:textColor">@color/white</item>

    </style>

    <style name="Theme.SparkWeather" parent="Base.Theme.SparkWeather" />
</resources>
